# Fallback de SPA: qualquer rota do roteador é servida pelo index.html.
# Sem isto, abrir /painel/licitacoes direto ou recarregar a página devolve 404,
# porque o arquivo não existe no disco — quem resolve a rota é o JavaScript.
# Usado por Netlify e por hospedagens compatíveis; na Vercel, ver vercel.json.
/*    /index.html   200
