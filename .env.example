# Em desenvolvimento: deixe vazio ou use "" — o proxy do Vite redireciona /api/* automaticamente
# Em produção: coloque a URL real do backend (ex: https://api.licitamatch.com.br)
VITE_API_URL=http://localhost:3000
